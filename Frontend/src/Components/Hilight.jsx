export default function Highlight({text}){
    return(
        
            
            
            <p className="  font-inter text-4xl font-semibold text-blue-100 inline  ">{`${text}`}</p>

     
       
    )
}